# Sdf2table

To compile:

1.  Install the following packages:
    - `build-essential`
    - `curl`
    - `pkg-config`
    - `graphviz`
2.  Compile and install ATerm in the default locations (from [Meta Environment][1])
3.  Ensure `libATerm.so` is on the dynamic library path

    ```
    find / -name libATerm.so
    # /usr/local/lib/libATerm.so
    echo $LD_LIBRARY_PATH
    # <empty>
    export $LD_LIBRARY_PATH=/usr/local/lib
    echo $LD_LIBRARY_PATH
    /usr/local/bin/baffle --help
    ```

4.  Configure this project using the following command:

    ```
    ./configure --with-cflags=-fgnu89-inline
    ```

5.  N/A

[1]: http://www.meta-environment.org/